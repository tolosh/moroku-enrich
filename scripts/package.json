{
  "name": "@moroku-enrich/scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Admin CLI scripts (ext-001): tenant key issuance + revocation. Run with tsx.",
  "scripts": {
    "build": "tsc --build",
    "issue-key": "tsx issue-tenant-key.ts",
    "revoke-key": "tsx revoke-tenant-key.ts"
  },
  "dependencies": {
    "@moroku-enrich/service-lib": "0.1.0"
  }
}
