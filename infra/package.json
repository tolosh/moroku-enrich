{
  "name": "infra",
  "version": "0.1.0",
  "private": true,
  "description": "Moroku Enrich AWS infrastructure (CDK, TypeScript) — spec §6.",
  "bin": {
    "moroku-enrich": "bin/moroku-enrich.ts"
  },
  "scripts": {
    "cdk": "cdk",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy:dev": "cdk deploy --context stage=dev --require-approval any-change"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "aws-cdk": "^2.170.0",
    "tsx": "^4.19.2"
  }
}
